summary: "Удалить вопрос"
description: "Удалить вопрос по его id"
consumes:
- "application/json"
produces:
- "application/json"
parameters:
- in: "body"
  name: "body"
  description: "other description"
  required: true
  schema:
    type: "object"
    properties:
      id:
        type: "integer"
        format: "int64"
        example : 5
responses:
  405:
    description: "Invalid input"